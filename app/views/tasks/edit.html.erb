<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <% if @task.creator == current_user # temp auth until we implement pundit %>
        <h1>Edit task</h1>
        <%= render "form", task: @task %>
      <% else %>
        <h1>You not not have permission to edit this task.</h1>
      <% end %>
    </div>
  </div>
</div>
